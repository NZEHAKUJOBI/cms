# Optional: add this block to /etc/nginx/sites-available/your-other-app
# (or a new file) if you ever want port 80 to route to the CMS via a path prefix,
# or if you add a domain later.
#
# For now the CMS is directly reachable at http://<VPS_IP>:8080
# and no Nginx changes are required.
#
# If you want VPS Nginx to proxy port 80/443 to the CMS later, use this:
#
# server {
#     listen 80;
#     server_name 164.68.120.153;
#
#     location / {
#         proxy_pass         http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 90s;
#     }
# }
